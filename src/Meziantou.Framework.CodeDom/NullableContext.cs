namespace Meziantou.Framework.CodeDom;

/// <summary>Specifies the nullable reference types context.</summary>
public enum NullableContext
{
    /// <summary>Uses the default nullable context.</summary>
    Default,

    /// <summary>Enables nullable reference types.</summary>
    Enable,

    /// <summary>Disables nullable reference types.</summary>
    Disable,
}
