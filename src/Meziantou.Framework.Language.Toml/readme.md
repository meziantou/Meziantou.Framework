# Meziantou.Framework.Language.Toml

A lossless, Roslyn-style immutable syntax tree for TOML 1.0 documents. Parsing preserves source text, comments, whitespace, quoted keys, arrays, inline tables, dates, and TOML draft extensions; malformed input is represented in the tree and reported through diagnostics.
