namespace Meziantou.Framework.Language.Tool;

/// <summary>The library that owns a language's syntax tree.</summary>
internal enum SyntaxLanguageFamily
{
    Ini,
    Json,
    Xml,
    Regex,
    Shell,
}
