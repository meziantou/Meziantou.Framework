namespace Meziantou.Framework;

/// <summary>Specifies the character casing for hexadecimal string conversion.</summary>
public enum HexaOptions
{
    /// <summary>Use lowercase letters (a-f) for hexadecimal digits.</summary>
    LowerCase,

    /// <summary>Use uppercase letters (A-F) for hexadecimal digits.</summary>
    UpperCase,
}
